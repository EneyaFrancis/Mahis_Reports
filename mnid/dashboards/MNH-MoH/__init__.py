"""MNH-MoH indicator definitions -- kept separate from the shared
(beginnings) and Nest360 indicator sets so a future role-based view can show
each dashboard its own indicators instead of one combined list. Package (not
a bare module) to match MNH-Nest360's structure, so both are loaded the same
way (mnid.dashboards.load_dashboard_module)."""
